version: '3.8'

services:
  web:
    build: .
    ports:
      - "8000:8000"
    environment:
      - TINKOFF_TOKEN=${TINKOFF_TOKEN:-Token}
    volumes:
      - .:/app
    command: uvicorn main:app --host 0.0.0.0 --port 8000 --reload
